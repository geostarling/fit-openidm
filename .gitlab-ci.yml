stages:
    - build
build_and_test:
    stage: build
    tags:
        - x86_64
    script:
        - mvn clean install
    artifacts:
        name: "${CI_BUILD_NAME}_${CI_BUILD_REF_NAME}"
        paths:
            - openidm-zip/target/openidm-*.zip
